# RAG backend dependencies. Install with:  pip install -r requirements.txt

# --- web framework ---
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9      # multipart/form-data PDF uploads

# --- config + validation ---
pydantic>=2
pydantic-settings>=2

# --- document ingestion ---
pypdf>=4                     # PDF text extraction

# --- embeddings + vector search (CPU) ---
sentence-transformers>=2.7   # all-MiniLM-L6-v2
faiss-cpu>=1.8
numpy>=1.26

# --- LLM client (points at the vLLM OpenAI-compatible server) ---
openai>=1.30
